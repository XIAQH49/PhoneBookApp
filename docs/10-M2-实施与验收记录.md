# PhoneBookApp M2 实施与验收记录

| 项 | 内容 |
|---|---|
| 文档版本 | v0.1 |
| 实施日期 | 2026-08-26 |
| 范围来源 | docs/09 第 3 节（M2-1 警告清零 / M2-2 Navigation 迁移 / M2-3 TextDecoder 新 API / M2-4 全量回归） |
| 基线版本 | v0.5.3（git de1700c） |
| 结论 | ✅ M2 编译与逻辑验证通过；真机回归（M2-4 第 3 节）待设备执行 |

---

## 1. 基线实测（M2 起点，clean 全量构建）

| 指标 | 值 |
|---|---|
| 编译错误 | 0 |
| 弃用警告 | 9（W-1 router×8 + W-2 decode×1） |
| may-throw 警告（W-3） | 116（CallListDao 70 / MetaDao 27 / StatusDao 10 / ConfigDao 6 / Index 3） |
| tools/verify | 39/39 |

## 2. 实施内容

| 任务 | 内容 | 涉及文件 | 验收结果 |
|---|---|---|---|
| M2-1 W-3 清零 | DAO 全部 SDK 调用点 try/catch 包装（失败抛带方法名 Error + finally 安全关闭 ResultSet），异常传播路径与事务语义不变 | database/CallListDao.ets、MetaDao.ets、StatusDao.ets、ConfigDao.ets | ✅ clean 构建 may-throw=0 |
| M2-2 W-1 迁移 | router→Navigation：Index 为宿主（Navigation+NavPathStack+pageMap）；ContactDetail/Settings/AssigneePicker 改 NavDestination（onReady 取参、pop(result)+onBackPressed 拦截保证宿主 onPop 刷新）；main_pages.json 仅保留 Index | pages/Index.ets、ContactDetail.ets、Settings.ets、AssigneePicker.ets、main_pages.json | ✅ clean 构建 router 弃用=0 |
| M2-3 W-2 替换 | SDK 26 查证：`decode` 弃用 since 9，替代 `decodeToString`（since 12）、带参构造弃用改用 `TextDecoder.create`；空输入兜底 | service/ImportService.ets | ✅ clean 构建 decode 弃用=0；Node 39 项含 CSV 编码检测用例全绿 |
| M2-4a 全量回归 | clean 全量构建 + tools/verify | — | ✅ 0 错误 0 警告；verify 39/39；HAP 正常产出（SignHap 受 CLI 签名限制见 docs/04） |
| M2-4b 真机回归 | 核心链路真机复验（清单见第 3 节） | — | ⬜ 待设备执行（IDE Run） |

### 2.1 Navigation 迁移后的功能等价说明（重点自查项）

- **导航**：列表→详情 / 责任人选择 / 设置，三处 pushPathByName 压栈；返回按钮与系统返回手势均触发 pop(result) → 宿主 onPop → 重载数据；
- **详情传参**：原 router.getParams 改为 onReady 读 pathInfo.param（id）；
- **拨号后标记**：详情页改为"唤起拨号即标记"（R-7 拨出即算已打，与原"返回后自动标记"结果一致）；主页"是否已拨打？"提示保持原样（宿主 onPageShow 在应用回前台时触发，不受迁移影响）；
- **责任人切换**：选择页写配置后 pop 带结果，宿主强制重载（等价于原 needReload+onPageShow 机制，且不再依赖页面生命周期回调）；
- **拨号/CSV 编码链路**：CallService 未改；CSV 解码仅替换等价 API，检测逻辑与合并逻辑零改动（verify 39 项覆盖）。

## 3. M2-4b 真机回归清单（IDE Run 安装后执行）

| 项 | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 导航-1 | 主页点条目进详情 → 返回按钮返回；再进详情 → 系统返回手势返回 | 列表正常显示、统计/筛选状态保留；返回后责任人视图与统计自动刷新 | |
| 导航-2 | 责任人▾ → 切换责任人 → 返回；⚙ → 设置 → 返回 | 切换立即生效且记住上次选择（重启仍保持）；设置页开关/清除可用 | |
| 导航-3 | 详情页改已打状态后返回列表 | 列表该条状态同步（onPop 重载生效） | |
| 拨号-1 | 列表单号码条目 → 确认框 → 拨打 | 系统拨号盘唤起且号码自动填入（viewData+tel 方案不变） | |
| 拨号-2 | 详情页拨打 → 返回 APP | 该条目已自动标记已打（唤起即标记，R-7） | |
| 拨号-3 | 多号码条目 → 选号弹层 → 拨打/复制 | 弹层正常；复制后剪贴板正确 | |
| 导入/导出 | 导入 2000 行 xlsx + GBK/UTF-8 CSV；标记 3 条 → 导出 → PC Excel 核对 | 导入无乱码；导出仅这 3 条"是否已打"=是，其余与原文件一致（重点：M2-3 decodeToString 后 GBK 检测仍正确） | |
| 内置名单 | 主页"内置名单"（无变化提示/强制导入） | 无变化→提示"无变化"；强制导入后总数正确 | |
| 一键清除 | 设置页清除全部名单 | 确认后数据清空、统计归零；再点"内置名单"可恢复 | |
| 稳定性 | 杀进程重启；深/浅色切换；hilog 过滤 PhoneBook 无 ERROR | 数据即时显示；界面随系统切换；无未捕获异常 | |

> 结果回填后：全过 → M2 验收完成（M2-4b ✅）；异常项 → 按 docs/09 坑位清单定位并在本表记录。

## 4. 验证命令与产物

```powershell
# 纯逻辑验证
node --import ./tools/verify/register.mjs tools/verify/verify.ts
# 编译（clean 全量；SignHap 因 CLI 签名限制报 Init keystore failed，属已知环境项）
$env:DEVECO_SDK_HOME = 'D:\DevEco Studio\sdk'
& 'D:\DevEco Studio\tools\node\node.exe' 'D:\DevEco Studio\tools\hvigor\bin\hvigorw.js' assembleHap --mode module -p product=default -p buildMode=debug --no-daemon
```

- 产物：`entry/build/default/outputs/default/entry-default-unsigned.hap`（约 2.9 MB）；真机安装统一走 DevEco Studio Run（自动签名）。
