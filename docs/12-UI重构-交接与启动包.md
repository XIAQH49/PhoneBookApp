# UI 重构交接与启动包（新会话无缝衔接用）

> 用途：当前会话上下文已长，新开会话继续"UI 大重构"版本时使用——
> 把下面第 2 节的提示词原样粘贴给新会话，新 AI 阅读本仓库文档与代码后即可按 SDD 流程开工，
> 无需重复任何历史沟通。

---

## 1. 如何"清上下文"（操作说明）

1. 新建一个对话；
2. 把下面第 2 节的提示词原样粘贴给新会话；
3. 新会话会读取本仓库的 SDD 文档链（docs/01~12）、README、git log 自行恢复全部背景。

## 2. 新会话启动提示词（复制粘贴）

```
继续开发 HarmonyOS NEXT 项目 PhoneBookApp，工作目录 D:\DevecoStudioProjects\PhoneBookApp。
这是接续此前多轮对话的新会话，你没有任何历史记忆，请先按顺序阅读：
  1) docs/12-UI重构-交接与启动包.md（本文档：现状快照、新需求、关键坑位、验证工作流）
  2) README.md 与 docs/ 目录全部文档（SDD 文档链：需求/设计/任务/构建/警告/验证/验收/内置名单/M2/M3 记录）
  3) git log --oneline 了解开发历史
然后按 SDD 流程实施"UI 大重构"版本（范围与风格见本文档第 3 节，已与用户拍板），
并用 tools/verify 与 hvigor 构建做验证，真机回归可用 uitest 自动化驱动（见第 6 节）。
```

## 3. 新需求：UI 大重构（已与用户拍板，2026-08-27）

| 项 | 决策 |
|---|---|
| 版本定位 | 下一个大版本 = **UI 重构**（功能面已基本完备，不再做大功能迭代） |
| 风格 | **简洁现代风**：大留白、卡片化、圆角、轻拟态（参考主流效率类 App） |
| 范围 | **大重构**：信息架构允许调整（如底部 Tab 导航、合并页面），但功能与数据语义不变 |
| 痛点1 | 主页布局：顶部标题/统计/筛选/搜索/列表/四个按钮全堆在一屏 → 重构信息层级 |
| 痛点2 | 列表行：信息平铺、徽标生硬、拨号按钮突兀 → 姓名+号码主视觉、状态胶囊化 |
| 痛点3 | 详情页：表格式逐行展示简陋 → 分区卡片（基本信息/号码区/状态区/备注区） |
| 痛点4 | 设置/选择页：样式与主流程割裂 → 统一组件与配色 |
| 痛点5 | 整体配色/字体/间距不统一 → 建立设计令牌（色板/字号/圆角/间距），深浅色都走资源引用 |

> 新会话须按 SDD 流程推进：更新 docs/01（UI 需求/线框）→ docs/02（设计令牌、组件规范、
> 信息架构）→ docs/03（任务拆分）→ 实现 → 验收。信息架构若大改，关键取舍先与用户确认。

## 4. 现状快照（截至 v0.6）

- 版本链：v0.3.4（M1 验收）→ v0.5.x 内置名单 → M2（警告清零/Navigation/decodeToString）→ M3（打通/意向可维护）→ v0.5.4（并发导入重复行修复）→ v0.6（一键导出原始文件）；
- 远端：https://github.com/XIAQH49/PhoneBookApp（main 与 origin/main 同步）；
- 验证基线：`node --import ./tools/verify/register.mjs tools/verify/verify.ts` = **46/46**；clean 全量构建 **0 错误、0 警告**；
- 真机：SGT-AL10（HarmonyOS 7.0.0.102 log，平板，USB/hdc）；M1/M2/M3/v0.6 均真机验收通过；
- 功能面：导入(xlsx/csv+编码检测)/责任人筛选/列表搜索统计/唤起拨号/标记已打+打通+意向/留档导出/内置名单/一键清除/一键导出原始文件 全部可用。

## 5. 关键坑位清单（历次会话累积，新会话必读）

1. **签名**：CLI hvigor 无法解密 IDE 加密口令（Init keystore failed）；签名+安装统一走 DevEco Studio Run；
2. **CLI 测试限制**：hvigorw test 不解析 src/main 导入；CLI 逻辑验证以 tools/verify Node 套件为准，hypium 仅 IDE 内跑；
3. **拨号**：零权限方案 = startAbility(want viewData + tel:URI + entity.system.home) 唤起 com.ohos.contacts；不用 queryAbilityByWant（需权限且废弃）；
4. **ArkUI 状态**：子组件参数必须 @Prop/@Link；LazyForEach 全量刷新必须用"数据源版本号并入条目键"（onDataReloaded 对同键条目复用视图不重建）；
5. **Navigation**：宿主 onPageShow 在 NavDestination pop 时不触发；子页 pop 必须带 result 且 onBackPressed 拦截返回 true，宿主经 onPop 回调刷新；pop 过渡期间宿主 List 隐藏，刷新要延迟 ~400ms；
6. **冷启动**：DB 异步初始化需 DbHelper.ready() 等待 + 页面重试；
7. **合并语义**：状态（已打/打通/意向）取"或"；源文件内完全重复的行原样保留（对半去重只消除并发导入多出的那一份）；新文件缺失行标记 in_file=0，重现时按同键复活；
8. **导入并发**：ImportService 有全局串行队列 + BuiltinListService 单飞，勿绕过 importFromBytes 直接写库；
9. **DB 迁移**：DbHelper.doInit 内幂等迁移（PRAGMA table_info + ALTER + app_config 标记守卫），新表结构变更必须走同一模式；
10. **rawfile/名单.xlsx**：本地构建用、已 gitignore；测试样本在 samples/（2000行/26行重复工号/GBK/UTF8/M3_20行）；
11. **真机自动化**：hdc shell uinput 注入触摸 + uitest dumpLayout 拿控件树（文本+坐标）；截图对非多模态模型无用；Toast/系统弹窗/文件选择器是独立窗口，dumpLayout 抓不到，需用户目视确认；
12. **hilog**：本设备上应用自定义域日志不出现在 hdc shell hilog 流中（现象未解），诊断以 UI 行为证据为准；
13. **git 代理**：推送依赖本机 Clash for Windows（E:\Clash for Windows\Clash for Windows.exe，端口 7890），代理没开时 git push 失败；
14. **构建环境**：DEVECO_SDK_HOME=D:\DevEco Studio\sdk、JAVA_HOME=D:\DevEco Studio\jbr；构建目录 .hvigor/entry/build 勿提交；
15. **ArkTS 语法**：对象字面量必须对应显式声明的 class/interface（arkts-no-untyped-obj-literals）；SDK 调用点必须 try/catch 否则 may-throw 警告，构建门禁 0 警告。

## 6. 验证工作流（新会话直接复用）

```powershell
# 纯逻辑验证（46 项）
node --import ./tools/verify/register.mjs tools/verify/verify.ts

# 编译（clean 全量；SignHap 报 Init keystore failed 属已知 CLI 限制，编译产物正常）
$env:DEVECO_SDK_HOME = 'D:\DevEco Studio\sdk'
& 'D:\DevEco Studio\tools\node\node.exe' 'D:\DevEco Studio\tools\hvigor\bin\hvigorw.js' assembleHap --mode module -p product=default -p buildMode=debug --no-daemon
```

- 真机安装：用户 DevEco Run（需用户配合）；
- UI 回归自动化：hdc uinput 点击 + uitest dumpLayout 断言控件文本/坐标（参考 tools/verify/device_*.ps1）；
- UI 重构验收：逐页对比设计线框 + 深浅色切换 + 核心链路回归（导入/筛选/拨号唤起/标记/导出/内置名单/清除/导出原始文件）。

## 7. 文档链索引

| 文档 | 内容 |
|---|---|
| docs/01 需求规格 v0.6 | FR-1~FR-12、NFR、9 项决议 |
| docs/02 设计规格 | 架构/数据表/流程/状态机/UI 线框（重构后需更新线框与设计令牌） |
| docs/03 任务清单 | T0~T20 + 构建门禁 + 进度记录 |
| docs/04 构建与真机调试指引 | CLI 构建、单测说明、权限说明 |
| docs/05 警告处置记录 | 0 警告基线（M2 清零） |
| docs/06 真机验证清单 / docs/07 M1验收 | 历史验收 |
| docs/08 内置名单说明 | v0.5~v0.6 行为与修复记录 |
| docs/09 M2交接 / docs/10 M2记录 / docs/11 M3记录 | 历次交接与验收 |
| docs/12 本文档 | UI 重构交接包 |
