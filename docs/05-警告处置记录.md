# PhoneBookApp 编译警告处置记录

| 项 | 内容 |
|---|---|
| 文档版本 | v0.1 |
| 关联 | SDD 构建门禁（`docs/03-任务清单.md` 1.1 节） |

原则：**0 编译错误；警告逐条评估**——能低成本修复的立即修复；结构性迁移登记为技术债任务并明确排期；评估结论记录在本文件，禁止未评估的警告堆积。

---

## 1. 已修复（2025 首次全量 UI 构建，142 条警告中的可修项）

| 类别 | 处置 | 涉及文件 |
|---|---|---|
| `PermissionRequestResult` 类型不存在（编译错误） | 改用类型推断，去除显式类型标注 | common/PermissionUtil.ets |
| `promptAction.showToast/showDialog` 已弃用 | 迁移至 `UIContext.getPromptAction()`（页面内私有 uiToast 封装 + try/catch） | pages/Index.ets、pages/ContactDetail.ets |
| `getContext(this)` 已弃用 | 新增 `common/ContextUtil.ets`：`UIContext.getHostContext()` 封装 + undefined 守卫 | pages/Index.ets、pages/ContactDetail.ets |
| fs 读写在无 catch 的 try/finally 中（"可能抛异常"） | readUri/writeUri 改为完整 try/catch/finally | service/ImportService.ets、service/ExportService.ets |

## 2. 接受并登记的技术债（结构性迁移，不影响 M1 功能）

| 编号 | 警告类别 | 数量 | 现状 | 影响评估 | 处置计划 |
|---|---|---|---|---|---|
| W-1 | `router.pushUrl/back/getParams` 已弃用 | 8 | 4 个页面使用 router 导航（pushUrl×3、back×4、getParams×1） | 功能正常，官方推荐迁往 Navigation；迁移需重构页面为 NavDestination 体系 | 排期 M3 或后续版本统一迁移（含动画与返回栈体验增强） |
| W-2 | `util.TextDecoder.decode` 已弃用 | 1 | CSV 解码适配器使用 decode | 功能正常；新版 API 行为差异需真机验证 | 随 T-2 真机 spike 一并验证新 API 后切换 |
| W-3 | DAO/服务层 SDK API "可能抛异常" | 104 | 异常由调用链传播至服务边界 try/catch（ImportService/ExportService/CallService 均已捕获），运行期行为安全 | 纯静态告警，无运行时风险 | 排期警告清零任务：逐方法包装或评估编译器配置项；不阻塞 M1 |

## 3. 复验记录

| 日期 | 构建方式 | 结果 | 说明 |
|---|---|---|---|
| 第 3 轮修复后 | clean 全量构建 | **0 错误**；9 条弃用（W-1/W-2）+ 104 条 may-throw（W-3） | 全部残留警告均已登记处置计划；构建产物正常生成 |
| 第 4 轮（xlsx 接入） | 增量构建 | **0 错误**；9 弃用 + 104 may-throw | 无新增类别 |
| 第 5 轮（拨号兼容开关/设置项/DAO 扩展） | 增量构建 | **0 错误**；9 弃用 + 109 may-throw | +5 条均属 W-3（StatusDao.getCalled、CallService、Settings 新代码），处置计划不变 |
