内置名单文件放置说明（v0.5）

将内网导出的名单文件重命名为以下之一，放入本目录（rawfile），随 APP 打包：
  - 名单.xlsx （优先）
  - 名单.csv

使用流程：
  1. 把最新名单文件放到本目录（覆盖旧文件即可）
  2. DevEco Studio 构建并 Run（重新安装）
  3. APP 启动时自动检测内容变化并合并导入（已打状态保留）
  4. 也可在 APP 主页点击"内置名单"按钮手动导入

注意：文件内容变化通过哈希检测；未变化时不会重复导入。
